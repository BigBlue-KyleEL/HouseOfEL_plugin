package com.houseofel.builder.death;

/**
 * The choice presented at exactly the 3rd scar — see the Death Policy plan. Persisted as
 * {@code helper_ledger.scar_choice}, null until (and unless) a Helper reaches 3 scars.
 */
public enum ScarChoice {
    WARY,
    HARDENED
}
